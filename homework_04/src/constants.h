#ifndef CONSTANTS_H
#define CONSTANTS_H

// iмпульсiв на один оберт колеса  
inline constexpr int ticks_per_revolution = 1024;

// радiус колеса у метрах (дiаметр 60 см)
inline constexpr float wheel_radius_m = 0.3;

// вiдстань мiж лiвим i правим бортом, у метрах
inline constexpr float wheelbase_m = 1.0;

#endif